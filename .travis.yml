language: python

python:
- 3.5

services:
- postgresql

before_install:
- export DATABASE_URL="postgres://postgres@localhost"

install:
- pip install -r requirements.txt

script:
- python manage.py test
